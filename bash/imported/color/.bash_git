## GIT ALIASES
alias gstat="git status"
alias gpull="git pull"
alias gadd="git add"
alias git_list="git help -a"  # OR git --help --all


## BASH COMPLETION
. ~/lib/git/.git-completion.bash


## GIT PS1 -
## SEE REPO STATUS DIRECTLY IN THE PROMPT
. ~/lib/git/.git-prompt.sh


# Change PS1 to call __git_ps1 as command-substitution
#  ->   Bash:
PS1='[\u@\h \W$(__git_ps1 " (%s)")]\$ '
#
#  ZSH:
#  setopt PROMPT_SUBST ; PS1='[%n@%m %c$(__git_ps1 " (%s)")]\$ '
#  the optional argument will be used as format string.
#
#  Alternatively, for a slightly faster prompt, __git_ps1 can be used
#  for PROMPT_COMMAND in Bash or for precmd() in Zsh with two parameters,
#  <pre> and <post>, which are strings
#  you would put in $PS1 before and after the status string
#  generated by the git-prompt machinery.  e.g.
#
#  Bash:
#  PROMPT_COMMAND='__git_ps1 "\u@\h:\w" "\\\$ "'
#  shows username @ host : cwd \ status string  &  and SP,
#
#  ZSH:
#  precmd () { __git_ps1 "%n" ":%~$ " "|%s" }
#  will show username | status string : cwd $ and SP
#
#  You can supply a third argument with a printf
#  format string to finetune the output of the branch status
